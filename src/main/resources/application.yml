azure:
  tenant:
    id: 694b6df5-fdfa-47a5-af86-9a18d87a4742
    name: dardeus3
  client-id: 3ef1d5ec-7f30-4d2b-b729-d12fa07d008a
  user-flow-name: B2C_1_ONE


spring:
  application:
    name: api-one
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: https://dardeus3.b2clogin.com/694b6df5-fdfa-47a5-af86-9a18d87a4742/v2.0/
#          audiences:
#            - 3ef1d5ec-7f30-4d2b-b729-d12fa07d008a
  cloud:
    azure:
      active-directory:
        b2c:
          enabled: true
          base-uri: https://${azure.tenant.name}.b2clogin.com             # Such as: https://xxxxb2c.b2clogin.com
          profile:
            tenant-id: ${azure.tenant.id}
#          app-id-uri: ${APP_ID_URI}         # If you're using v1.0 token, configure app-id-uri for `aud` verification
          credential:
            client-id: ${azure.client-id}           # If you're using v2.0 token, configure client-id for `aud` verification
          user-flows:
            sign-up-or-sign-in: ${azure.user-flow-name}
logging:
  level:
    root: info
    com.github.rubenqba.apione: debug
    org:
      springframework:
        web: DEBUG
        boot:
          autoconfigure:
            security: DEBUG
        security.oauth2: DEBUG
        security.oauth2.client: DEBUG
        security.oauth2.server.resource: DEBUG